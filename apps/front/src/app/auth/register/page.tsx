import { RegisterView } from '@front/sections/auth';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Jwt: Register',
};

export default function RegisterPage() {
  return <RegisterView />;
}
